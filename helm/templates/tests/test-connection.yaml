apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "r4c-cesium-viewer.fullname" . }}-test-connection"
  labels:
    {{- include "r4c-cesium-viewer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "r4c-cesium-viewer.fullname" . }}:{{ .Values.frontend.service.port }}']
  restartPolicy: Never
