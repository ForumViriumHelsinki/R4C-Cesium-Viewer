apiVersion: v1
kind: Service
metadata:
  name: {{ include "r4c.fullname" . }}
  labels:
    {{- include "r4c.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "r4c.selectorLabels" . | nindent 4 }}
