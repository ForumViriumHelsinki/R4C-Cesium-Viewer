apiVersion: v1
kind: Secret
metadata:
  name: r4c-cesium-viewer-secrets
type: Opaque
stringData:
  # ⚠️ REQUIRED: These values MUST be set for the application to function correctly!
  #
  # These are currently empty and will cause the application to fail at runtime
  # if they are not properly configured. You have several options:
  #
  # Option 1: Edit this file directly (not recommended for sensitive values)
  # Option 2: Create the secret manually before running Skaffold:
  #   kubectl create secret generic r4c-cesium-viewer-secrets \
  #     --from-literal=VITE_DIGITRANSIT_KEY="your-key" \
  #     --from-literal=SENTRY_AUTH_TOKEN="your-token" \
  #     --from-literal=SENTRY_DSN="your-dsn"
  # Option 3: Use environment variables with envsubst or Kustomize
  #
  # See k8s/README.md for more details.
  VITE_DIGITRANSIT_KEY: ""
  SENTRY_AUTH_TOKEN: ""
  SENTRY_DSN: ""
