apiVersion: v1
kind: Secret
metadata:
  name: r4c-cesium-viewer-migration-secret
  labels:
    app.kubernetes.io/name: r4c-cesium-viewer
    app.kubernetes.io/instance: r4c-cesium-viewer
    app.kubernetes.io/component: migration
type: Opaque
stringData:
  # ⚠️ WARNING: These are default credentials for LOCAL DEVELOPMENT ONLY!
  # DO NOT use these credentials in production or commit custom credentials to git.
  # For production, create a separate secret with secure credentials.
  password: "regions4climate_pass"
  admin-password: "postgres"
