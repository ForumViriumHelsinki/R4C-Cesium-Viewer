apiVersion: v1
kind: Secret
metadata:
  name: r4c-cesium-viewer-migration-secret
  namespace: regions4climate
  labels:
    app.kubernetes.io/name: r4c-cesium-viewer
    app.kubernetes.io/instance: r4c-cesium-viewer
    app.kubernetes.io/component: migration
type: Opaque
stringData:
  # This template is auto-populated from .env using envsubst
  # For local development, defaults are set in scripts/generate-secrets.sh
  # For production, set DB_PASSWORD and DB_ADMIN_PASSWORD in your .env
  password: "regions4climate_pass"
  admin-password: "postgres"
